# Base URL of the FlowDesk backend API (must include the /v1 prefix from the OpenAPI spec).
# This value is baked into the SPA at BUILD time by Vite — change it and rebuild.
VITE_API_BASE_URL=http://localhost:8080/v1

# Host port the frontend container will be exposed on (maps to nginx :80 inside).
FRONTEND_PORT=3000